{
  "name": "@stream-heaven/shared-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utility helpers for Stream Heaven services (Phase 1 stub)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.2"
  }
}
